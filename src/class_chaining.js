/*
 Sometimes I frequently chain a lot of classes in a same way.
 Instead of repeating, I use this module to store my favorite class combinations.
 (Earlier there were more, but later I simplified the app and there are now only 2 left.)
*/

const chainedClasses = {
    head: 'Head',
    my_card: 'card text-body margin-1',
};

export default chainedClasses;
